{
    "name": "database",
    "version": "0.0.0",
    "main": "./index.ts",
    "types": "./index.ts",
    "scripts": {
        "db:generate": "prisma generate --schema=./prisma/schema.prisma",
        "db:migrate-dev": "prisma migrate dev --schema=./prisma/schema.prisma --name dev"
    },
    "dependencies": {
        "@prisma/client": "5.4.2"
    },
    "devDependencies": {
        "prisma-dbml-generator": "0.10.0",
        "prisma": "5.4.2"
    }
}
